<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.RookieWZW.dao.OrderSettingDao">
    <insert id="add" parameterType="com.RookieWZW.pojo.OrderSetting">
        insert into t_orderSetting(orderDate,number,reservations)
        values
        (#{orderDate},#{number},#{reservations})
    </insert>

    <update id="editNumberByOrderDate" parameterType="com.RookieWZW.pojo.OrderSetting">
        update t_ordersetting set number = #{number} where orderDate = #{orderDate}
    </update>

    <select id="findCountByOrderDate" parameterType="java.util.Date" resultType="long">
        select count(*) from t_ordersetting where orderDate = #{orderDate}
    </select>n

    <select id="getOrderSettingByMonth" parameterType="hashmap" resultType="com.RookieWZW.pojo.OrderSetting">
        select * from t_ordersetting where orderDate between  #{orderBegin} and #{orderEnd}
    </select>

    <select id="findByOrderDate" parameterType="date" resultType="com.RookieWZW.pojo.OrderSetting">
        select * from t_ordersetting where orderDate=#{orderDate}
    </select>

    <update id="editReservationsByOrderDate" parameterType="com.RookieWZW.pojo.OrderSetting">
        update t_ordersetting set reservation = #{reservations} where orderDate = #{orderDate}
    </update>
</mapper>